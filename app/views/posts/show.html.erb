<div class="container">
  <div class="row">
    <h1><%= link_to 'Blog', root_path %></h1>
    <div class="col-8 offset-4">
      <h2><%= @post.title %></h2>
      <p><%= @post.content %></p>
      <small><%= @post.created_at.strftime("geschrieben am %d.%m.%Y") %></small>
      <small>von <%= link_to @post.author.name, author_path(@post.author) %></small>
    </div>
  </div>
</div>


